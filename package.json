{
  "name": "afterlight-radio",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "Premium 12-room place-based listening experience for Cloudflare Workers",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "check:worker": "wrangler deploy --dry-run --outdir .wrangler-dry-run",
    "test": "npm run build && npm run check && npm run check:worker",
    "serve": "python3 -m http.server 4173 -d public",
    "cf:dev": "npm run build && wrangler dev",
    "cf:deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@neondatabase/serverless": "1.1.0"
  },
  "devDependencies": {
    "wrangler": "4.34.0"
  }
}
